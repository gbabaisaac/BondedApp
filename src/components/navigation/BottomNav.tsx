// This component is not used - navigation is handled by MobileLayout
// Keeping file for reference but exporting empty component to avoid build errors
import React from 'react';

export const BottomNav: React.FC = () => {
  return null;
};
